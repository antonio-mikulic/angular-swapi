import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class NotifyService {
  // Example of how messaging service could be made...
  // Could be used for showing toast messages, etc.
  constructor() {}
}
